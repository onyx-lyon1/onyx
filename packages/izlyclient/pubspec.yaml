name: izlyclient
description: A Dart library for interacting with the Izly platform, allowing you to perform various operations such as retrieving balances, generating QR codes, recharging accounts, and more.
version: 1.0.6
repository: https://github.com/onyx-lyon1/izlyclient

environment:
  sdk: '>=3.0.0 <4.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  dotenv: ^4.0.1
  build_runner: ^2.4.6
  dart_mappable_builder: ^4.0.1

dependencies:
  requests_plus: ^4.8.3
  html: ^0.15.4
  beautiful_soup_dart: ^0.3.0
  dart_mappable: ^4.0.1
