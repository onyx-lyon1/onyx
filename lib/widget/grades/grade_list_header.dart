import 'package:flutter/material.dart';

class GradeListHeader extends StatelessWidget {
  const GradeListHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
