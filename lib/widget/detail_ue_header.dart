import 'package:flutter/material.dart';

class DetailUeHeader extends StatelessWidget {
  const DetailUeHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
